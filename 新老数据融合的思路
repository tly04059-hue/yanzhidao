分三步走：

1. 先替换页面里的 16 条硬编码假数据 → 替换成 student-cases-publish.json 的 60 条真实数据（schema 差距最小，字段已经是展示层设计，工作量最小）
2. 案例页面的数据来源统一到 student-cases-publish.json，这个文件专门为小程序展示设计，字段完整
3. cases.json 保持为推荐引擎专用，只在结果页"相似案例"（§4.8 区块 5）和 K-NN 匹配时消费，不直接作为案例库展示的数据源

这样两个文件各司其职，不需要融合，也不需要替换——只需要把页面里那 16 条假数据换成真实的 student-cases-publish.json 就能解决当前问题。
