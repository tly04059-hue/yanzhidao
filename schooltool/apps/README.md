# Code Zone

当前项目的代码区按两条实现主线拆分：

- `旧版原型/`
  早期 Node/Express + SQLite Web 原型。
  适合继续参考旧推荐逻辑、数据库结构和接口雏形。

- `小程序/`
  当前移动端主线，基于 uni-app。
  主要用于小程序 / H5 手机界面体验和后续移动端迭代。

## 当前建议

- 新的移动端页面与交互，优先在 `小程序/` 继续推进
- `旧版原型/` 保留为旧原型和规则参考，不再作为主要前端方向
- 共享数据文件继续放在根目录 `data/`
