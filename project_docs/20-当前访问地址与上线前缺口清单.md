# 当前访问地址与上线前缺口清单

> 更新时间：2026-05-22  
> 本次目标：走一遍小程序、内部院校库、数据中控流水线，确认当前可访问入口和上线前缺口。

## 1. 当前可访问地址

本机开发地址：

- 小程序 H5 开发服务：`http://localhost:5173/#/pages/index/index`
- 内部院校库页面：`http://localhost:8091/yanzhidao_schooldata.html`

2026-05-22 临时联调地址：

- 小程序 H5 开发服务（局域网可访问）：`http://192.168.3.56:5174/#/pages/index/index`
- 小程序测一测：`http://192.168.3.56:5174/#/pages/test/index`
- 小程序结果页链路预览入口：先从测一测页进入

同一局域网同事可访问地址：

- 小程序 H5 构建版：`http://192.168.3.56:8092/#/pages/index/index`
- 小程序院校库：`http://192.168.3.56:8092/#/pages/schools/index`
- 小程序案例库：`http://192.168.3.56:8092/#/pages/cases/index`
- 小程序测一测：`http://192.168.3.56:8092/#/pages/test/index`
- 内部院校库页面：`http://192.168.3.56:8091/yanzhidao_schooldata.html`

注意：

1. 上述局域网地址只在当前电脑开机、服务运行、同事和本机处于同一网络时可访问。
2. `5173` 当前只监听本机，不作为同事访问入口。
3. 正式提测/上线仍需域名、HTTPS、微信小程序后台合法域名和后端接口地址。

## 2. 本次已跑通

`projectOps` 一键流水线：

```bash
python3 projectOps/scripts/release.py --target h5
python3 projectOps/scripts/build-miniapp.py --target mp-weixin
```

校验结果：

- H5 构建通过。
- 微信小程序构建通过。
- 院校库发布数据 JSON 可读。
- 案例库发布数据 JSON 可读。
- 推荐方案库发布数据 JSON 可读。
- 学生案例发布层隐私扫描通过。

数据概况：

- 院校发布记录：234 条。
- MPA：95 条。
- MEM：97 条。
- MBA：42 条。
- 覆盖省份：四川、重庆、云南、贵州。
- 学生案例：60 条。
- 推荐策略：6 条，覆盖 MPA、MEM、MBA、党校。

## 3. 页面巡检结果

已确认可正常显示：

- 首页。
- 了解页。
- 院校库。
- 院校详情。
- 案例库。
- 测一测入口。
- 推荐结果页。
- 路径对比页。
- 备考时间线。
- 内部院校库 `yanzhidao_schooldata.html`。

当前占位或不完整：

- 估分工具：页面是占位说明，用户已确认本版暂不处理，后续有内容后再增加。
- 留资提交：页面有入口，但正式可用依赖后端接口和域名配置。
- 分享海报：目前是提示弹窗，未生成正式分享海报。

## 4. 上线前 P0 缺口

1. 后端接口与正式域名  
   当前 H5 构建版可浏览，但留资、埋点、反馈等接口不能继续使用 `127.0.0.1`。需要准备正式 API 域名、HTTPS 和微信合法域名配置。

2. 留资链路  
   需要确认手机号/微信提交后的存储位置、销售通知方式、隐私说明和失败兜底。

3. 页面冒烟测试自动化  
   目前是人工/浏览器抽检。上线前建议把首页、了解、院校库、详情、案例库、测一测、结果页写入 `projectOps` 自动冒烟检查。

4. 党校完整院校库  
   当前内部页面已有四川/重庆党校数据，小程序院校库尚未完整并入党校路径。用户已确认该项与推荐方案库细化同步处理，等待同事补齐党校详细内容后推进。

5. MEM 补齐  
   MEM 已进入小程序院校库，但源 HTML 缺部分学费、学制、复试线、上课地点、调剂和录取率，前台会显示“待定”。用户已确认本版暂不补齐，放到第二版或第三版。

6. 微信小程序真机提测  
   `build:mp-weixin` 已通过，但仍需导入微信开发者工具，检查授权、合法域名、真机样式和网络请求。

7. 测一测页 V5 结构缺口  
   当前 `miniapp/src/pages/test/index.vue` 的答题阶段还没有完全对齐 V5。已确认缺口包括：
   - 答题阶段顶部缺少“已服务 1,000+ 川渝同学 · 不骗人 · 真服务”。
   - 答题阶段顶部缺少“测一测”标题。
   - 答题阶段顶部返回箭头样式和位置仍不是 V5 口径。
   - 第一问 8 个选项当前仍是网格布局，需改成一项一行。
   - 底部 Tab 需要在答题阶段继续保留。

8. 结果页同行匹配数据完整度不足  
   `peer-insights` 已切到结构化字段优先，也已加入标签完整度权重和 top case 完整度兜底；但党校案例在 `system_tag / position_tag / goal_tag / party_member_tag` 上仍大量缺失，`5a / 5b` 结果还不够稳。

## 5. 当前确认的版本取舍

本版继续推进：

1. 推荐策略扩容。该项是本版重点，但依赖党校详细数据，等同事准备好党校内容后继续细化。
2. 党校院校库并入小程序。与推荐策略扩容同步处理，等党校详细数据确认后推进。
3. 院校库视觉对齐。下一步按 `schooltool/docs/ui_samples` 统一小程序和院校库样式。
4. 正式 API 和域名。
5. 留资闭环。
6. 微信开发者工具真机测试。

2026-05-15 进展：

- 院校库、院校详情、了解页已按样稿做第一轮视觉收口。
- 生产 API 地址已统一为 `https://yanzhidao.com/api`。
- 微信 URL 合法域名校验已开启。
- 已新增 `projectOps/scripts/check-production-readiness.py`，用于检查生产 API、微信 AppID、URL 校验和微信构建目录。
- 微信 AppID 已写入：`wx72386d5026c9782a`。
- 当前生产就绪检查本地项已通过；线上域名检测发现 `https://yanzhidao.com` 握手失败、`http://yanzhidao.com` 返回阿里云备案拦截页，DNS/备案/HTTPS、微信后台合法域名、真机测试和销售承接仍需人工确认。

本版暂不推进：

1. 估分工具正式化。当前保留占位，后续有内容后再增加。
2. 案例库筛选增强。本版案例数据不再更新，筛选增强放到第二版或第三版。
3. MEM 字段补齐。放到第二版或第三版。

## 6. 上线前 P1 缺口

1. 路径对比数据化  
   当前路径对比偏静态展示，后续应接入院校库和推荐策略库，支持党校 vs MPA、MPA vs MEM、MBA vs MEM 等组合。

2. 分享海报和资料领取  
   需要补充海报样式、二维码、资料包路径和客服承接方式。

3. 案例库 P0 标签补录  
   先补 `30-35`、`四川党校`、结果页最容易命中的那批案例，优先补：
   - `system_tag`
   - `position_tag`
   - `goal_tag`
   - `party_member_tag`

## 7. 今日巡检结论（2026-05-22）

已完成主线：

- 首页、了解页、路径对比、测一测入口、9 题、DP001、结果页 8 个 V5 区块均已接入当前小程序源码。
- 结果页同行匹配已从全文命中升级为“结构字段优先 + 文本兜底”。
- 学生案例库 L1/L2 schema、生成脚本、发布 JSON 已落地 `persona / system_tag / position_tag / goal_tag / party_member_tag`。

仍未完成或未闭环：

- 微信开发者工具真机联调还没做最终验收。
- 测一测页答题阶段还没完全对齐 V5 顶部结构与 Q1 布局。
- 党校案例 P0 标签不足，导致结果页 `5a / 5b` 可信度提升有限。
- 留资、正式域名、微信合法域名、真机网络链路仍属于上线前阻断项。

当前已确认 bug / 缺陷：

1. 测一测答题阶段 UI 缺口如上。
2. `5a 相似个案` 在党校路径下仍容易命中“30-35 / 其他事业编 / 综合岗 / goal 待确认”的低信息量案例。
3. `5b 同画像分布` 在同一组固定答案下仍可能出现 `6 / 6` 打平，根因是党校案例标签太薄，不是结果页组件错误。
4. 本地 H5 联调仍有埋点 warning，需在正式环境复核。

## 8. 后续建议

下一步优先级：

1. 先修 `测一测` 页答题阶段 V5 缺口：品牌行、标题、返回箭头、Q1 一行一项、底部 Tab 保留。
2. 导入微信开发者工具做真机测试，确认上述改动在 `mp-weixin` 端展示一致。
3. 补党校案例 P0 标签，优先处理 `30-35 + 四川党校 + 高频命中样本`。
4. 同步推进正式后端/API 域名和留资链路。
5. 等党校详细资料就绪后，再并入党校院校库并扩展推荐策略库。
6. 同步建立 `projectOps` 页面冒烟测试。
