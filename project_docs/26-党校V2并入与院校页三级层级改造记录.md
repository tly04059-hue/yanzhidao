# 党校 V2 并入与院校页三级层级改造记录

> 更新时间：2026-05-18  
> 涉及目录：`schoolData/`、`miniapp/`、`projectOps/`

## 1. 本次目标

1. 基于新增党校数据源：
   - `schoolData/sourceData/party/四川党校在职研究生信息库V2.xlsx`
   - `schoolData/sourceData/party/重庆党校在职研究生信息库V2.xlsx`
2. 更新院校库 L1/L2，并同步到小程序数据源。
3. 调整小程序“院校查询”层级：
   - 一级：`MPA`、`MEM`、`MBA`、`党校`
   - 二级：`四川`、`重庆`、`云南`、`贵州`
   - 三级：各地区下院校列表
4. 在“院校查询”中，`sc-avatar` 使用图片图标替换文字。
5. 调整全局 `page-v3` 默认留白策略，避免影响页面分区模块的满宽布局。

## 2. 数据更新结果

执行脚本：

```bash
python3 AIAgent/scripts/build_party_school_v2.py
```

产出确认：

- `schoolData/standardized/party/party_school_l1_v2.json`
- `schoolData/publish/party_school_miniapp_publish.json`
- `miniapp/src/data/party-school-miniapp-publish.json`
- `schoolData/dx_data.json`

构建报告显示：

- 党校 L1 记录数：8
- 党校 L2 发布数：8
- 四川党校专业数：3
- 重庆党校专业数：5

## 3. 小程序改造点

### 数据层

- 修改 `miniapp/src/data/school-data.ts`
  - 新增 `resolveSchoolLogo(record)`，按项目和院校返回图标路径。
  - `SchoolListItem.logoUrl`、`SchoolDetail.logoUrl` 从空字符串改为真实图片路径。

### 院校查询页

- 修改 `miniapp/src/pages/schools/index.vue`
  - 筛选从旧混合 chip 改为三级结构的前两级（项目/省份），第三级为院校卡片列表。
  - `sc-avatar` 由文字改为 `<image>` 图标。
  - 调整了筛选条、卡片、统计条的尺寸，使信息更紧凑。
  - 底部 tab 图标改为图片资源。

### 图标资源

- 新增到 `miniapp/src/static/`：
  - `school-mpa.png`
  - `school-mem.png`
  - `school-mba.png`
  - `school-party-sc.png`
  - `school-party-cq.png`
  - `tab-home.png`
  - `tab-home-active.png`
  - `tab-learn.png`
  - `tab-learn-active.png`
  - `tab-test.png`
  - `tab-test-active.png`

### 首页 / 了解页图标替换

- 修改 `miniapp/src/pages/index/index.vue`
  - 三状态入口卡片和工具入口图标改为图片。
  - 底部 tab 图标改为图片。
- 修改 `miniapp/src/pages/learn/index.vue`
  - 维度卡、系统卡、建议卡、对比卡、库入口、底部 tab 改为图片图标。
  - `cta-bar` 调整为紧贴 tab 上方（`bottom: 72px`）。

### 全局间距策略

- 修改 `miniapp/src/styles/v3.scss`
  - `page-v3` 默认横向内边距由 `16px` 改为 `0`，仅保留底部留白。
  - 页面模块间距改为各页面局部样式控制，避免影响 `advice-section` 等满宽模块。

## 4. 构建与校验

已执行：

```bash
npm run type-check
python3 projectOps/scripts/build-miniapp.py --target h5
python3 projectOps/scripts/build-miniapp.py --target mp-weixin
npm run format:mp-pages
python3 projectOps/scripts/check-all.py
```

结果：全部通过。

## 5. 后续注意事项

1. `schooltool/` 仍保持只读，不在该目录做任何修改。
2. 图标资源已本地化到 `miniapp/src/static/`，后续可替换为正式品牌图标，不影响当前结构。
3. 二级省份当前为固定顺序 `四川/重庆/云南/贵州`，某一级项目下若无数据会显示空状态。
